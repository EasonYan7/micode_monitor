prefix_rule(
    pattern = ["Fetching content from https://github.com/trending and processing with prompt: \"请提取当前 GitHub Trending 页面上排名前5的热门仓库信息，包括：仓库名称、描述、语言、star 数量、fork 数量和作者信息。\""],
    decision = "allow",
)

prefix_rule(
    pattern = ["Writing to abc.md"],
    decision = "allow",
)

prefix_rule(
    pattern = ["Fetching content from https://github.com/trending and processing with prompt: \"请提取当前 GitHub Trending 页面上排名前10的热门仓库信息，包括：仓库名称、描述、语言、star 数量、fork 数量、作者信息，以及它们的更新时间、主要贡献者和项目特点。\""],
    decision = "allow",
)

prefix_rule(
    pattern = ["curl -s \"https://github.com/trending\" | grep -A 5 -B 5 'h3 class=\"h3\"' | head -50 (Fetching GitHub trending page to get popular repositories)"],
    decision = "allow",
)

prefix_rule(
    pattern = ["curl -s \"https://api.github.com/search/repositories?q=created:>2026-02-08&sort=stars&order=desc&per_page=10\" | jq -r '.items[] | \"\\(.full_name) - \\(.description) - Stars: \\(.stargazers_count)\"' (Fetching GitHub trending repositories using GitHub API)"],
    decision = "allow",
)

prefix_rule(
    pattern = ["curl -s \"https://api.github.com/search/repositories?q=created:>2026-02-08&sort=stars&order=desc&per_page=10\" | jq -r '.items[] | \"### \\(.full_name)\\n**\\(.description // \"No description\")**\\n- **Language:** \\(.language // \"Unknown\")\\n- **Stars:** \\(.stargazers_count)\\n- **Forks:** \\(.forks_count)\\n- **Author:** \\(.owner.login)\\n- **URL:** \\(.html_url)\\n\"' > /tmp/repo_info.txt (Getting detailed repository information)"],
    decision = "allow",
)
